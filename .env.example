SLACK_BOT_TOKEN=some_value
SLACK_APP_TOKEN=some_value
BROWSERBIRD_AUTH_TOKEN=some_value

# Agent authentication (pick one):
# API key (recommended) — pay-per-token via console.anthropic.com
ANTHROPIC_API_KEY=some_value
# OAuth token (personal use) — uses your Claude Pro/Max subscription
# CLAUDE_CODE_OAUTH_TOKEN=some_value
